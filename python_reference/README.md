# Python Reference

This directory keeps the earlier Python backend implementation for learning and reference only.

The active app is the static browser version in the repository root:

- `../index.html`
- `../style.css`
- `../app.js`

The static app does not import, request, or depend on any file in this directory.
